# Local development environment variables
# Copy to .env.local and update values as needed

# Backend service URLs (for docker-compose local dev)
BACKEND_URL=http://backend:8080
LIGHTCURVE_API_URL=http://lightcurve:9000

# Optional shared secret for server-to-server auth
# Leave empty for local development
INTERNAL_AUTH_TOKEN=

# Next.js public variables (if needed for client-side)
# Not needed for production with proxy routes
# NEXT_PUBLIC_BACKEND_BASE=/api/proxy
# NEXT_PUBLIC_LIGHTCURVE_BASE=/api/lightcurve